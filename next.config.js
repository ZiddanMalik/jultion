/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: [
            "files.edgestore.dev",
            "convex.cloud",
            "helpful-clam-121.convex.cloud",
        ]
    }
}

module.exports = nextConfig
